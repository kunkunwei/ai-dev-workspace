# 检查点 — 2026-01-20

> 硬约束：≤ 40 行 / ≤ 1200 中文字符（由 `tools/ws-lint.cjs` 强制）。
> 检查点只回答两件事：**上次做到哪**、**下一步是什么**。不写历史全文。

## 刚完成
- `ws-lint.cjs` 增加 4 项带预算的检查，并把阈值分级为"告警/失败"两档
- 把 3 个同类现场故障的判据抽成共性流程（设备按物理路径匹配，不按编号）
- 索引生成改为机器产出（`ws-index.cjs`），并在 lint 里加"索引新鲜度"检查

## 关键事实（别再重摸）
- 设备节点别名必须按**物理端口路径**匹配；按总线编号匹配会在换端口后静默失效
- `respawn` 只在**进程退出**时触发，它不是健康检查——进程卡死但没退出时永远不会被救
- 无保电 RTC 的机器，一次开机墙钟可能跳变 3~4 次；所有时间线分析一律用**单调时钟**

## 未完成 / 下一步
1. ⏳ 把"物理路径匹配"写成 procedure，含 30 秒分层判据
2. ⏳ 增加 lint 检查：禁止配置里出现按编号匹配的写法
3. ⏳ 观察预算阈值一周，评估误报率

## 边界
- 本次为纯文档与工具改动，未触碰任何设备或远端环境
- 未合并分支、未改历史
